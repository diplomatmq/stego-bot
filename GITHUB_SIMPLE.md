# 📤 Простая инструкция - Загрузка на GitHub

## 🖥️ Вариант 1: GitHub Desktop (проще всего)

### Шаг 1: Выберите опцию
В GitHub Desktop выберите:
**"Add an Existing Repository from your local drive..."**
(четвертый пункт снизу)

### Шаг 2: Укажите папку
- Нажмите "Choose..."
- Выберите папку: `C:\Users\dip663322o2244\PycharmProjects\stego`
- Нажмите "Add repository"

### Шаг 3: Проверьте файлы
- Слева увидите список файлов
- **Убедитесь, что НЕ отмечены**:
  - ❌ `.env`
  - ❌ `giveaway.db`
  - ❌ `giveaway_session.session`
  - ❌ `ssl/` (папка)
  - ❌ `venv/` (папка)

### Шаг 4: Сделайте коммит
- Внизу введите сообщение: "Initial commit"
- Нажмите "Commit to main"

### Шаг 5: Опубликуйте
- Нажмите "Publish repository"
- Выберите имя: `stego-bot` (или любое другое)
- Выберите Private или Public
- Нажмите "Publish repository"

**Готово! ✅**

---

## 💻 Вариант 2: Через CMD (командная строка)

### Шаг 1: Откройте CMD в папке проекта

**Способ A:**
1. Откройте папку `C:\Users\dip663322o2244\PycharmProjects\stego` в проводнике
2. В адресной строке введите `cmd` и нажмите Enter

**Способ B:**
1. Нажмите Win+R
2. Введите `cmd` и нажмите Enter
3. Выполните:
   ```cmd
   cd C:\Users\dip663322o2244\PycharmProjects\stego
   ```

### Шаг 2: Проверьте, установлен ли Git

```cmd
git --version
```

Если ошибка - скачайте Git: https://git-scm.com/download/win

### Шаг 3: Инициализируйте репозиторий

```cmd
git init
```

### Шаг 4: Проверьте, что секретные файлы игнорируются

```cmd
git status
```

**Должны быть только:**
- ✅ `.py` файлы
- ✅ `.html`, `.css`, `.js` файлы
- ✅ `.md` файлы
- ✅ `requirements.txt`

**НЕ должно быть:**
- ❌ `.env`
- ❌ `giveaway.db`
- ❌ `giveaway_session.session`

Если видите `.env` или `.db` - они уже в `.gitignore`, но проверьте еще раз.

### Шаг 5: Добавьте файлы

```cmd
git add .
```

### Шаг 6: Сделайте коммит

```cmd
git commit -m "Initial commit"
```

### Шаг 7: Создайте репозиторий на GitHub.com

1. Откройте https://github.com
2. Войдите в аккаунт
3. Нажмите **"+"** → **"New repository"**
4. Имя: `stego-bot`
5. Выберите **Private** или **Public**
6. **НЕ** отмечайте "Initialize with README"
7. Нажмите **"Create repository"**

### Шаг 8: Подключите и загрузите

GitHub покажет команды. Выполните (замените YOUR_USERNAME):

```cmd
git remote add origin https://github.com/YOUR_USERNAME/stego-bot.git
git branch -M main
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub (или токен).

**Готово! ✅**

---

## ⚠️ Важно

### Что НЕ должно попасть в GitHub:
- ❌ `.env` - токены и пароли
- ❌ `giveaway.db` - база данных
- ❌ `giveaway_session.session` - сессия Telethon
- ❌ `ssl/` - SSL сертификаты
- ❌ `venv/` - виртуальное окружение

Все это уже в `.gitignore`, но проверьте перед загрузкой!

---

## 🆘 Проблемы

### "git is not recognized"
- Скачайте Git: https://git-scm.com/download/win
- Перезапустите CMD после установки

### "Permission denied" при push
- Создайте токен доступа: https://github.com/settings/tokens
- Используйте токен вместо пароля

### Случайно добавили .env
```cmd
git rm --cached .env
git commit -m "Remove .env"
git push
```

---

**Выберите удобный вариант и следуйте инструкциям! 🚀**

