# 🖥️ Настройка сервера - Пошаговая инструкция

## 📋 Что нужно сделать на сервере

### Вариант 1: Автоматическая настройка (Cloud-init)

Если ваш провайдер поддерживает cloud-init (большинство VPS провайдеров):

1. При создании VPS загрузите файл `cloud-init.yaml`
2. Или выполните вручную после создания сервера:

```bash
# Загрузите cloud-init.yaml на сервер
scp cloud-init.yaml root@ваш_ip:/tmp/

# На сервере выполните
sudo cloud-init -f /tmp/cloud-init.yaml
```

### Вариант 2: Ручная настройка через консоль

Подключитесь к серверу через SSH или веб-консоль провайдера:

```bash
ssh root@90.156.211.211
# Введите пароль (который вы получили при создании VPS)
```

#### Шаг 1: Обновление системы

```bash
apt update
apt upgrade -y
```

#### Шаг 2: Установка Python 3.13

```bash
# Для Ubuntu 22.04
apt install software-properties-common -y
add-apt-repository ppa:deadsnakes/ppa -y
apt update
apt install python3.13 python3.13-venv python3.13-dev -y

# Для Debian 12
apt install python3.11 python3.11-venv python3.11-dev -y
```

#### Шаг 3: Установка дополнительных пакетов

```bash
apt install git curl build-essential openssl nginx ufw -y
```

#### Шаг 4: Создание пользователя для бота (рекомендуется)

```bash
# Создаем пользователя
adduser botuser
# Введите пароль (или оставьте пустым для SSH ключа)

# Добавляем в группу sudo
usermod -aG sudo botuser

# Переключаемся на пользователя
su - botuser
```

#### Шаг 5: Настройка firewall

```bash
# Разрешаем SSH (важно сделать первым!)
ufw allow 22/tcp

# Разрешаем порт для бота
ufw allow 8000/tcp

# Разрешаем HTTP/HTTPS (если будете использовать Nginx)
ufw allow 80/tcp
ufw allow 443/tcp

# Включаем firewall
ufw --force enable

# Проверяем статус
ufw status
```

#### Шаг 6: Создание директории для бота

```bash
# Если вы под пользователем botuser
mkdir -p ~/stego-bot
cd ~/stego-bot

# Если вы под root
mkdir -p /home/botuser/stego-bot
chown -R botuser:botuser /home/botuser/stego-bot
```

---

## 📤 Загрузка файлов на сервер

После настройки сервера загрузите файлы бота:

### Способ 1: Через скрипт deploy.sh

```bash
# На вашем компьютере
./deploy.sh botuser@90.156.211.211
```

### Способ 2: Вручную через SCP

```bash
# На вашем компьютере
scp *.py *.html *.css *.js requirements.txt generate_ssl.py botuser@90.156.211.211:~/stego-bot/
scp .env botuser@90.156.211.211:~/stego-bot/
```

### Способ 3: Через веб-консоль провайдера

1. Откройте веб-консоль в панели управления VPS
2. Используйте встроенный файловый менеджер или редактор
3. Создайте файлы вручную

---

## ⚙️ Настройка бота на сервере

После загрузки файлов подключитесь к серверу:

```bash
ssh botuser@90.156.211.211
cd ~/stego-bot
```

### 1. Создание виртуального окружения

```bash
python3.13 -m venv venv
# или для Python 3.11:
# python3.11 -m venv venv
```

### 2. Активация окружения

```bash
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 4. Настройка .env файла

```bash
nano .env
```

Убедитесь, что заполнены:
- `BOT_TOKEN` - токен от @BotFather
- `CREATOR_ID` - ваш Telegram ID
- `WEBAPP_URL` - https://90.156.211.211 (ваш IP)

### 5. Создание SSL сертификата

```bash
python generate_ssl.py 90.156.211.211
```

### 6. Тестовый запуск

```bash
python bot.py
```

Если все работает, нажмите `Ctrl+C` и переходите к настройке автозапуска.

---

## 🔄 Настройка автозапуска (systemd)

```bash
# Создаем файл сервиса
sudo nano /etc/systemd/system/stego-bot.service
```

Вставьте (замените пути на ваши):

```ini
[Unit]
Description=Stego Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/stego-bot
Environment="PATH=/home/botuser/stego-bot/venv/bin"
ExecStart=/home/botuser/stego-bot/venv/bin/python /home/botuser/stego-bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable stego-bot.service
sudo systemctl start stego-bot.service
sudo systemctl status stego-bot.service
```

---

## ✅ Проверка работы

```bash
# Проверка статуса
sudo systemctl status stego-bot.service

# Просмотр логов
sudo journalctl -u stego-bot.service -f

# Проверка порта
curl http://localhost:8000
```

---

## 🆘 Частые проблемы

### Не могу подключиться по SSH

1. Проверьте, что firewall разрешает порт 22
2. Проверьте IP адрес сервера
3. Убедитесь, что используете правильный пароль/SSH ключ

### Ошибка "Permission denied"

```bash
# Проверьте права доступа
ls -la ~/stego-bot
chmod +x bot.py
```

### Бот не запускается

```bash
# Проверьте логи
sudo journalctl -u stego-bot.service -n 50

# Проверьте .env файл
cat .env

# Проверьте виртуальное окружение
source venv/bin/activate
python --version
```

---

## 📝 Чек-лист настройки

- [ ] Сервер создан и доступен по SSH
- [ ] Python 3.13 установлен
- [ ] Пользователь botuser создан
- [ ] Firewall настроен (порты 22, 8000 открыты)
- [ ] Файлы бота загружены
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл настроен
- [ ] SSL сертификат создан
- [ ] Бот запускается в тестовом режиме
- [ ] Systemd service создан и запущен
- [ ] Бот работает после перезагрузки

---

**Готово! Бот должен работать на вашем сервере! 🚀**

