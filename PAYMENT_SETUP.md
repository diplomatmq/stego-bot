# 💳 Настройка реальной оплаты в Telegram

## Почему работает тестовый режим?

Тестовый режим активируется, когда:
- Приложение открыто напрямую в браузере (не через Telegram)
- Telegram WebApp API недоступен
- Бот не настроен для работы с платежами

## ✅ Как подключить реальную оплату

### 1. Открытие приложения через бота

Приложение должно открываться **только через бота в Telegram**, а не напрямую в браузере.

В `bot.py` уже настроено открытие через WebApp:
```python
web_app = WebAppInfo(url=f"{public_https_url}?tg_id={telegram_id}&_v={cache_buster}")
```

### 2. Настройка бота для платежей

#### Шаг 1: Включить платежи в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Payments"
5. Включите платежи (Enable Payments)

#### Шаг 2: Настроить провайдера платежей

Для **Telegram Stars** (⭐):
- Платежи работают автоматически после включения в BotFather
- Никаких дополнительных настроек не требуется

Для **TON** (💎):
- Убедитесь, что у вас настроен TON кошелек в `.env`:
  ```
  TON_WALLET=ваш_адрес_кошелька
  ```

Для **NFT Gifts** (🎁):
- Убедитесь, что у вас настроен CREATOR_ID в `.env`:
  ```
  CREATOR_ID=ваш_telegram_id
  ```

### 3. Проверка доступности Telegram WebApp

При открытии приложения через бота в консоли браузера должны появиться:
```
✅ Telegram WebApp обнаружен
✅ Telegram WebApp инициализирован
📱 Платформа: [платформа]
👤 Пользователь: [данные пользователя]
```

Если этих сообщений нет, значит приложение открыто не через Telegram WebApp.

### 4. Проверка работы платежей

1. Откройте приложение **через бота** в Telegram
2. Перейдите в магазин
3. Нажмите "Купить" на любом товаре
4. Выберите способ оплаты
5. Если Telegram WebApp доступен, откроется реальное окно оплаты
6. Если WebApp недоступен, появится тестовый режим

### 5. Настройка вебхука для проверки платежей

В `web_server.py` уже есть endpoint `/api/payment/webhook` для получения уведомлений о платежах.

Для настройки вебхука в BotFather:
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/setmenubutton`
3. Выберите вашего бота
4. Укажите URL вашего вебхука: `https://ваш-домен.com/api/payment/webhook`

### 6. Проверка в production

Для production убедитесь, что:
- ✅ Используется HTTPS (обязательно для WebApp)
- ✅ URL в `bot.py` указывает на production домен
- ✅ Бот настроен в BotFather для платежей
- ✅ Все переменные окружения настроены

### 7. Отладка

Если платежи не работают:

1. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Посмотрите на сообщения в консоли
   - Должны быть сообщения о Telegram WebApp

2. **Проверьте, что приложение открыто через бота:**
   - URL должен содержать параметры от Telegram
   - `window.Telegram.WebApp` должен быть доступен

3. **Проверьте настройки бота:**
   - Платежи включены в BotFather
   - Бот не заблокирован

4. **Проверьте серверные логи:**
   - Смотрите логи в `web_server.py`
   - Проверьте endpoint `/api/payment/verify`

## 📝 Важные замечания

- ⚠️ **Тестовый режим** работает только для разработки
- ✅ **Реальная оплата** работает только когда приложение открыто через бота в Telegram
- 🔒 Все платежи проверяются на сервере через `/api/payment/verify`
- 💰 Для Stars и TON используется `Telegram.WebApp.openInvoice()`
- 🎁 Для NFT Gifts требуется специальная настройка в BotFather

## 🔗 Полезные ссылки

- [Telegram WebApp Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Payments Documentation](https://core.telegram.org/bots/payments)
- [BotFather](https://t.me/BotFather)


