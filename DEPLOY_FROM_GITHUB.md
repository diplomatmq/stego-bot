# 🚀 Загрузка проекта на сервер из GitHub

## 📋 Варианты загрузки

### Вариант 1: Клонирование из GitHub (рекомендуется)

Если репозиторий **Private**, используйте один из способов ниже.

#### Способ A: Через SSH ключ (самый удобный)

**1. Создайте SSH ключ на сервере:**

```bash
# На сервере
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для всех вопросов (или введите пароль)

# Покажите публичный ключ
cat ~/.ssh/id_ed25519.pub
```

**2. Добавьте ключ на GitHub:**

1. Скопируйте содержимое `~/.ssh/id_ed25519.pub`
2. Перейдите: https://github.com/settings/keys
3. Нажмите "New SSH key"
4. Вставьте ключ и сохраните

**3. Клонируйте репозиторий:**

```bash
# На сервере
cd ~
git clone git@github.com:diplomatmq/stego-bot.git
cd stego-bot
```

#### Способ B: Через Personal Access Token

**1. Создайте токен на GitHub:**

1. Перейдите: https://github.com/settings/tokens
2. "Generate new token" → "Generate new token (classic)"
3. Отметьте `repo` (полный доступ)
4. Скопируйте токен

**2. Клонируйте с токеном:**

```bash
# На сервере
cd ~
git clone https://YOUR_TOKEN@github.com/diplomatmq/stego-bot.git
cd stego-bot
```

Или:

```bash
git clone https://github.com/diplomatmq/stego-bot.git
# При запросе:
# Username: diplomatmq
# Password: YOUR_TOKEN (не пароль!)
```

#### Способ C: Через GitHub CLI

```bash
# Установите GitHub CLI
sudo apt install gh -y

# Авторизуйтесь
gh auth login

# Клонируйте
gh repo clone diplomatmq/stego-bot
cd stego-bot
```

---

### Вариант 2: Загрузка через SCP (если не хотите использовать Git)

**С вашего компьютера:**

```bash
# Загрузите все файлы на сервер
scp -r * botuser@ваш_ip:~/stego-bot/
```

**⚠️ ВАЖНО**: Не загружайте `.env`, `giveaway.db`, `ssl/` - создайте их на сервере!

---

### Вариант 3: Использование скрипта deploy.sh

**С вашего компьютера:**

```bash
# Загрузите файлы через скрипт
./deploy.sh botuser@ваш_ip
```

---

## ⚙️ Настройка на сервере после клонирования

### 1. Создайте .env файл

```bash
cd ~/stego-bot
nano .env
```

Вставьте:

```env
BOT_TOKEN=ваш_токен_бота
CREATOR_ID=ваш_telegram_id
WEBAPP_URL=https://ваш_ip_адрес
```

### 2. Создайте виртуальное окружение

```bash
python3.13 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Создайте SSL сертификат

```bash
python generate_ssl.py ваш_ip_адрес
```

### 4. Настройте Telethon (если используется)

```bash
python setup_telethon_session.py
```

### 5. Запустите бота

```bash
python bot.py
```

---

## 🔄 Обновление кода на сервере

Если вы обновили код на GitHub:

```bash
# На сервере
cd ~/stego-bot
git pull origin main

# Перезапустите бота
sudo systemctl restart stego-bot.service
```

---

## 📝 Полная инструкция

1. **Создайте репозиторий на GitHub** (Private или Public)
2. **Загрузите код на GitHub** (с вашего компьютера)
3. **Клонируйте на сервер** (один из способов выше)
4. **Настройте .env** на сервере
5. **Установите зависимости** и запустите бота

---

## ✅ Преимущества клонирования из GitHub

- ✅ Легко обновлять код (`git pull`)
- ✅ Версионирование изменений
- ✅ Резервная копия кода
- ✅ Можно работать с нескольких компьютеров

---

**Рекомендую использовать Вариант 1 (клонирование) - это удобнее для обновлений! 🚀**

