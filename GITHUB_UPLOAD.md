# 📤 Инструкция по загрузке проекта на GitHub

## ✅ Что нужно загрузить на GitHub

### Обязательные файлы (Python код):
```
bot.py
web_server.py
db.py
models.py
config.py
helpers.py
giveaway.py
creator.py
cryptobot.py
collection.py
picture.py
post_parser.py
randomizer.py
telethon_comments.py
setup_telethon_session.py
generate_ssl.py
requirements.txt
```

### HTML/CSS/JS файлы (веб-интерфейс):
```
prob.html
user.html
admin.html
creator.html
index.html
style.css
script.js
```

### Документация:
```
README.md (создайте, если нет)
DEPLOYMENT.md
QUICK_START.md
SETUP_SERVER.md
GITHUB_UPLOAD.md
PAYMENT_SETUP.md
MIGRATION_TO_AIogram2.md
README_TELETHON.md
```

### Скрипты для деплоя:
```
deploy.sh
setup_server.sh
cloud-init.yaml
```

---

## ❌ Что НЕ нужно загружать на GitHub

### Секретные данные (уже в .gitignore):
```
.env                    # Токены и пароли
.env.local
.env.*.local
giveaway_session.session # Сессия Telethon
*.session
```

### База данных:
```
giveaway.db
*.db
*.sqlite
*.sqlite3
```

### SSL сертификаты:
```
ssl/
*.pem
*.key
*.crt
```

### Временные файлы:
```
__pycache__/
*.pyc
*.pyo
venv/
.venv/
env/
```

### Логи:
```
*.log
logs/
```

### IDE файлы:
```
.vscode/
.idea/
*.swp
```

---

## 🚀 Как загрузить на GitHub

### Вариант 1: Через GitHub Desktop (проще всего)

1. **Скачайте GitHub Desktop**: https://desktop.github.com/
2. **Войдите в GitHub** через приложение
3. **Создайте новый репозиторий**:
   - File → New Repository
   - Имя: `stego-bot` (или любое другое)
   - Local Path: выберите папку с проектом
   - ✅ Initialize this repository with a README (опционально)
4. **Проверьте файлы**:
   - Убедитесь, что `.env`, `giveaway.db`, `ssl/` не отмечены для загрузки
5. **Сделайте коммит**:
   - Введите сообщение: "Initial commit"
   - Нажмите "Commit to main"
6. **Опубликуйте**:
   - Нажмите "Publish repository"

### Вариант 2: Через командную строку (Git)

#### Шаг 1: Установите Git (если еще не установлен)

**Windows:**
- Скачайте: https://git-scm.com/download/win
- Установите с настройками по умолчанию

**Linux/Mac:**
```bash
# Ubuntu/Debian
sudo apt install git

# Mac
brew install git
```

#### Шаг 2: Инициализируйте репозиторий

```bash
# Перейдите в папку проекта
cd C:\Users\dip663322o2244\PycharmProjects\stego

# Инициализируйте Git
git init

# Проверьте .gitignore (убедитесь, что секретные файлы игнорируются)
cat .gitignore
```

#### Шаг 3: Добавьте файлы

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что добавилось (должны быть только нужные файлы)
git status
```

**⚠️ ВАЖНО**: Убедитесь, что `.env`, `giveaway.db`, `ssl/` НЕ в списке!

#### Шаг 4: Сделайте первый коммит

```bash
git commit -m "Initial commit: Telegram bot for giveaways"
```

#### Шаг 5: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"+"** → **"New repository"**
4. Заполните:
   - **Repository name**: `stego-bot` (или любое другое)
   - **Description**: "Telegram bot for managing giveaways and contests"
   - **Visibility**: 
     - ✅ **Public** (если хотите, чтобы код был открытым)
     - ✅ **Private** (если хотите скрыть код)
   - ❌ **НЕ** отмечайте "Initialize with README" (у нас уже есть файлы)
5. Нажмите **"Create repository"**

#### Шаг 6: Подключите локальный репозиторий к GitHub

GitHub покажет инструкции. Выполните:

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/stego-bot.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите файлы на GitHub
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub (или токен доступа).

---

## 🔐 Настройка доступа (если нужен токен)

Если Git просит токен вместо пароля:

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note**: "Local Git"
   - **Expiration**: выберите срок действия
   - **Scopes**: отметьте `repo` (полный доступ к репозиториям)
4. Нажмите **"Generate token"**
5. **Скопируйте токен** (он показывается только один раз!)
6. Используйте токен вместо пароля при `git push`

---

## 📝 Создание README.md (рекомендуется)

Создайте файл `README.md` с описанием проекта:

```markdown
# Stego Bot - Telegram Bot for Giveaways

Telegram бот для управления конкурсами и розыгрышами.

## 🚀 Быстрый старт

1. Клонируйте репозиторий
2. Установите зависимости: `pip install -r requirements.txt`
3. Настройте `.env` файл
4. Запустите бота: `python bot.py`

## 📚 Документация

- [Быстрый старт](QUICK_START.md)
- [Деплой на сервер](DEPLOYMENT.md)
- [Настройка сервера](SETUP_SERVER.md)

## ⚙️ Требования

- Python 3.11+
- aiogram 2.25.1
- FastAPI
- SQLAlchemy

## 📄 Лицензия

Ваша лицензия
```

---

## ✅ Чек-лист перед загрузкой

- [ ] `.env` файл НЕ добавлен в Git (проверьте `git status`)
- [ ] `giveaway.db` НЕ добавлен в Git
- [ ] `ssl/` папка НЕ добавлена в Git
- [ ] `giveaway_session.session` НЕ добавлен в Git
- [ ] Все Python файлы добавлены
- [ ] Все HTML/CSS/JS файлы добавлены
- [ ] Документация добавлена
- [ ] `.gitignore` настроен правильно
- [ ] README.md создан (рекомендуется)

---

## 🔄 Обновление репозитория

После изменений в коде:

```bash
# Проверьте изменения
git status

# Добавьте измененные файлы
git add .

# Сделайте коммит
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

---

## 🆘 Проблемы

### Ошибка "remote origin already exists"

```bash
# Удалите старый remote
git remote remove origin

# Добавьте заново
git remote add origin https://github.com/YOUR_USERNAME/stego-bot.git
```

### Случайно добавили секретные файлы

```bash
# Удалите из индекса (но оставьте локально)
git rm --cached .env
git rm --cached giveaway.db

# Добавьте в .gitignore (если еще нет)
echo ".env" >> .gitignore
echo "giveaway.db" >> .gitignore

# Сделайте коммит
git commit -m "Remove sensitive files"
git push
```

### Не могу загрузить (push rejected)

```bash
# Получите последние изменения
git pull origin main --rebase

# Попробуйте снова
git push
```

---

## 📦 Альтернатива: ZIP архив

Если не хотите использовать Git, можете:

1. Создать ZIP архив с нужными файлами
2. Загрузить на GitHub через веб-интерфейс:
   - Создайте репозиторий
   - Нажмите "uploading an existing file"
   - Загрузите ZIP и распакуйте

**⚠️ ВАЖНО**: Не включайте `.env`, `giveaway.db`, `ssl/` в архив!

---

**Готово! Ваш проект на GitHub! 🎉**

