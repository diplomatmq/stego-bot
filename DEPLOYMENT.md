# 🚀 Руководство по публикации бота на сервере

## 📋 Содержание
1. [Требования](#требования)
2. [Подготовка файлов](#подготовка-файлов)
3. [Настройка сервера](#настройка-сервера)
4. [Загрузка файлов на сервер](#загрузка-файлов-на-сервер)
5. [Настройка окружения](#настройка-окружения)
6. [Запуск бота](#запуск-бота)
7. [Автозапуск при перезагрузке](#автозапуск-при-перезагрузке)
8. [Настройка веб-сервера (опционально)](#настройка-веб-сервера-опционально)
9. [Мониторинг и логи](#мониторинг-и-логи)
10. [Решение проблем](#решение-проблем)

---

## 📦 Требования

### Что нужно иметь:
- ✅ VPS сервер (например, от DigitalOcean, Hetzner, Timeweb, Selectel)
- ✅ SSH доступ к серверу
- ✅ Python 3.13 (или 3.11+)
- ✅ Токен бота от @BotFather
- ✅ API credentials для Telethon (если используется)

### Рекомендуемые характеристики сервера:
- **CPU**: 1-2 ядра
- **RAM**: 1-2 GB
- **Диск**: 10-20 GB
- **ОС**: Ubuntu 22.04 LTS или Debian 12

---

## 📁 Подготовка файлов

### 1. Создайте `.env` файл (если его нет)

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Токен бота от @BotFather
BOT_TOKEN=ваш_токен_бота

# ID создателя (ваш Telegram ID)
CREATOR_ID=ваш_telegram_id

# База данных (по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///giveaway.db

# WebApp URL (IP адрес вашего сервера с HTTPS)
# Замените на IP вашего сервера (например: https://90.156.211.211)
WEBAPP_URL=https://ваш_ip_адрес

# Telethon (опционально, если используете)
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_PHONE=+79991234567

# Платежи (опционально)
TON_WALLET=ваш_ton_кошелек
CRYPTOBOT_API_TOKEN=ваш_cryptobot_токен
```

**⚠️ ВАЖНО**: 
- НЕ публикуйте `.env` файл в Git!
- Добавьте `.env` в `.gitignore`

### 2. Файлы для загрузки на сервер

Загрузите следующие файлы и папки:

**Обязательные файлы:**
```
bot.py
web_server.py
db.py
models.py
config.py
helpers.py
giveaway.py
creator.py
cryptobot.py
collection.py
picture.py
post_parser.py
randomizer.py
telethon_comments.py
setup_telethon_session.py
requirements.txt
generate_ssl.py
```

**HTML файлы (для веб-интерфейса):**
```
prob.html
user.html
admin.html
creator.html
index.html
style.css
script.js
```

**Дополнительные файлы:**
```
AoT.jpg (если используется)
giveaway.db (если уже есть база данных)
giveaway_session.session (если уже настроен Telethon)
```

**Папки:**
```
drawing_uploads/ (если есть загруженные работы)
```

---

## 🖥️ Настройка сервера

### 1. Подключитесь к серверу по SSH

```bash
ssh root@ваш_ip_адрес
# или
ssh username@ваш_ip_адрес
```

### 2. Обновите систему

```bash
sudo apt update
sudo apt upgrade -y
```

### 3. Установите Python 3.13 (или используйте 3.11+)

**Для Ubuntu 22.04:**
```bash
sudo apt install software-properties-common -y
sudo add-apt-repository ppa:deadsnakes/ppa -y
sudo apt update
sudo apt install python3.13 python3.13-venv python3.13-dev -y
```

**Для Debian 12:**
```bash
sudo apt install python3.11 python3.11-venv python3.11-dev -y
```

### 4. Установите дополнительные зависимости

```bash
sudo apt install git curl build-essential -y
```

### 5. Создайте пользователя для бота (рекомендуется)

```bash
sudo adduser botuser
sudo usermod -aG sudo botuser
su - botuser
```

---

## 📤 Загрузка файлов на сервер

### Способ 1: Через SCP (рекомендуется для первого раза)

**С вашего компьютера:**

```bash
# Создайте папку для бота на сервере
ssh botuser@ваш_ip "mkdir -p ~/stego-bot"

# Загрузите все файлы
scp -r bot.py web_server.py db.py models.py config.py *.py botuser@ваш_ip:~/stego-bot/
scp -r *.html *.css *.js botuser@ваш_ip:~/stego-bot/
scp requirements.txt botuser@ваш_ip:~/stego-bot/
scp .env botuser@ваш_ip:~/stego-bot/

# Если есть папки
scp -r drawing_uploads botuser@ваш_ip:~/stego-bot/
```

### Способ 2: Через Git (если проект в репозитории)

```bash
# На сервере
cd ~
git clone https://github.com/ваш_username/ваш_репозиторий.git stego-bot
cd stego-bot

# Создайте .env файл
nano .env
# Вставьте содержимое .env файла
```

### Способ 3: Через SFTP (FileZilla, WinSCP)

1. Подключитесь к серверу через SFTP
2. Загрузите все файлы в папку `~/stego-bot/`

---

## ⚙️ Настройка окружения

### 1. Перейдите в папку проекта

```bash
cd ~/stego-bot
```

### 2. Создайте виртуальное окружение

```bash
python3.13 -m venv venv
# или для Python 3.11:
# python3.11 -m venv venv
```

### 3. Активируйте виртуальное окружение

```bash
source venv/bin/activate
```

### 4. Установите зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 5. Настройте .env файл

```bash
nano .env
```

Убедитесь, что все переменные заполнены:
- `BOT_TOKEN` - токен от @BotFather
- `CREATOR_ID` - ваш Telegram ID (можно узнать у @userinfobot)
- `WEBAPP_URL` - URL вашего сервера с HTTPS (например: `https://90.156.211.211`)
- Остальные переменные по необходимости

**⚠️ ВАЖНО**: `WEBAPP_URL` должен быть с HTTPS! Используйте IP адрес вашего сервера.

### 6. Создайте SSL сертификат (обязательно для HTTPS)

Telegram WebApp требует HTTPS. Создайте самоподписанный SSL сертификат:

```bash
# Установите OpenSSL (если еще не установлен)
sudo apt install openssl -y

# Создайте SSL сертификат (замените IP на ваш)
python generate_ssl.py 90.156.211.211

# Или если IP уже указан в .env
python generate_ssl.py
```

Сертификаты будут созданы в папке `ssl/`:
- `ssl/key.pem` - приватный ключ
- `ssl/cert.pem` - сертификат

**Примечание**: Самоподписанный сертификат может вызвать предупреждение в браузере, но Telegram WebApp будет работать.

### 7. Настройте Telethon (если используется)

```bash
python setup_telethon_session.py
```

Следуйте инструкциям для создания сессии.

---

## 🚀 Запуск бота

### Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Запустите бота
python bot.py
```

Если все работает, вы увидите логи запуска. Нажмите `Ctrl+C` для остановки.

### Запуск в фоновом режиме (screen/tmux)

**С помощью screen:**
```bash
sudo apt install screen -y
screen -S stego-bot
source venv/bin/activate
python bot.py
# Нажмите Ctrl+A, затем D для отсоединения
```

**Для повторного подключения:**
```bash
screen -r stego-bot
```

**С помощью tmux:**
```bash
sudo apt install tmux -y
tmux new -s stego-bot
source venv/bin/activate
python bot.py
# Нажмите Ctrl+B, затем D для отсоединения
```

**Для повторного подключения:**
```bash
tmux attach -t stego-bot
```

---

## 🔄 Автозапуск при перезагрузке

### Создание systemd service (рекомендуется)

1. Создайте файл сервиса:

```bash
sudo nano /etc/systemd/system/stego-bot.service
```

2. Вставьте следующее содержимое:

```ini
[Unit]
Description=Stego Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/stego-bot
Environment="PATH=/home/botuser/stego-bot/venv/bin"
ExecStart=/home/botuser/stego-bot/venv/bin/python /home/botuser/stego-bot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**⚠️ ВАЖНО**: Замените `botuser` на ваше имя пользователя и пути на актуальные!

3. Перезагрузите systemd:

```bash
sudo systemctl daemon-reload
```

4. Включите автозапуск:

```bash
sudo systemctl enable stego-bot.service
```

5. Запустите сервис:

```bash
sudo systemctl start stego-bot.service
```

6. Проверьте статус:

```bash
sudo systemctl status stego-bot.service
```

**Полезные команды:**
```bash
# Остановить бота
sudo systemctl stop stego-bot.service

# Перезапустить бота
sudo systemctl restart stego-bot.service

# Посмотреть логи
sudo journalctl -u stego-bot.service -f
```

---

## 🌐 Настройка веб-сервера (опционально)

Если вы хотите, чтобы веб-интерфейс был доступен по домену с HTTPS.

### 1. Установите Nginx

```bash
sudo apt install nginx -y
```

### 2. Настройте Nginx

```bash
sudo nano /etc/nginx/sites-available/stego-bot
```

Вставьте:

```nginx
server {
    listen 80;
    server_name ваш_домен.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 3. Активируйте конфигурацию

```bash
sudo ln -s /etc/nginx/sites-available/stego-bot /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 4. Настройте SSL (Let's Encrypt)

```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d ваш_домен.com
```

---

## 📊 Мониторинг и логи

### Просмотр логов systemd

```bash
# Все логи
sudo journalctl -u stego-bot.service

# Последние 100 строк
sudo journalctl -u stego-bot.service -n 100

# Логи в реальном времени
sudo journalctl -u stego-bot.service -f

# Логи за сегодня
sudo journalctl -u stego-bot.service --since today
```

### Проверка работы бота

```bash
# Проверьте, что процесс запущен
ps aux | grep bot.py

# Проверьте порт веб-сервера (если используется)
curl http://localhost:8000
```

---

## 🔧 Решение проблем

### Бот не запускается

1. **Проверьте логи:**
```bash
sudo journalctl -u stego-bot.service -n 50
```

2. **Проверьте .env файл:**
```bash
cd ~/stego-bot
cat .env
```

3. **Проверьте виртуальное окружение:**
```bash
source venv/bin/activate
python --version
pip list
```

### Ошибка "Module not found"

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка подключения к базе данных

Проверьте права доступа к файлу базы данных:
```bash
chmod 644 giveaway.db
```

### Бот не отвечает

1. Проверьте, что бот запущен:
```bash
sudo systemctl status stego-bot.service
```

2. Проверьте токен бота в .env

3. Проверьте интернет-соединение на сервере

### Веб-интерфейс не открывается

1. Проверьте, что веб-сервер запущен:
```bash
curl http://localhost:8000
```

2. Проверьте порт в bot.py (по умолчанию 8000)

3. Проверьте firewall:
```bash
sudo ufw status
sudo ufw allow 8000/tcp
```

---

## 📝 Чек-лист перед запуском

- [ ] Все файлы загружены на сервер
- [ ] Python 3.13 (или 3.11+) установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Все зависимости установлены (`pip install -r requirements.txt`)
- [ ] `.env` файл создан и заполнен
- [ ] Telethon сессия настроена (если используется)
- [ ] База данных инициализирована
- [ ] Бот запускается в тестовом режиме
- [ ] Systemd service создан и настроен
- [ ] Автозапуск включен
- [ ] Логи проверены на ошибки

---

## 🆘 Получить помощь

Если возникли проблемы:

1. Проверьте логи: `sudo journalctl -u stego-bot.service -f`
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность токенов в .env
4. Убедитесь, что порты не заняты другими процессами

---

## 📚 Дополнительные ресурсы

- [Документация aiogram](https://docs.aiogram.dev/)
- [Документация FastAPI](https://fastapi.tiangolo.com/)
- [Systemd руководство](https://www.freedesktop.org/software/systemd/man/systemd.service.html)

---

**Удачи с деплоем! 🚀**

